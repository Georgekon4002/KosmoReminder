# Οδηγός Ανάπτυξης (Deployment Guide) - KosmoReminder

Αυτός ο οδηγός περιγράφει τα βήματα και τις απαιτήσεις για την τελική εγκατάσταση του συστήματος KosmoReminder στο διαγνωστικό κέντρο της Κοσμοϊατρικής. Απευθύνεται στους διαχειριστές του συστήματος και στο IT.

## 1. Απαραίτητα Στοιχεία & Διαπιστευτήρια

Πριν την εγκατάσταση, θα πρέπει να συγκεντρωθούν και να ρυθμιστούν τα εξής στο αρχείο `.env`:

*   **easysms.gr API Key:** Το κλειδί API για την αποστολή SMS/Viber από το [easysms.gr](https://easysms.gr/). Επιπλέον, πρέπει να είναι εγκεκριμένα τα Sender IDs (`VIBER_SENDER_ID` και `SMS_SENDER_ID`), π.χ. "Kosmoiatriki".
*   **Callback URL:** Ένα δημόσια προσβάσιμο URL (π.χ. `https://sms.kosmoiatriki.gr/api/sms-callback`) το οποίο θα καλεί το easysms.gr για να ενημερώνει το σύστημα αν το μήνυμα παραδόθηκε επιτυχώς (Delivery Reports). Αυτό προϋποθέτει ότι ο κεντρικός server (ή ένα router/firewall μπροστά του) θα "ακούει" προς τα έξω στο port/path της εφαρμογής, πιθανότατα μέσω κάποιου reverse proxy (π.χ. Nginx ή IIS).
*   **Resend API Key (Email):** Κλειδί API από το [Resend.com](https://resend.com) για την αποστολή των Emails επιβεβαίωσης. **Προσοχή:** Πρέπει να έχει ρυθμιστεί και να έχει επαληθευτεί (verified domain) το **Domain της Κοσμοϊατρικής** (π.χ. `@kosmoiatriki.gr`) στο panel του Resend. Χωρίς verified domain, τα email είτε δεν θα φεύγουν καθόλου είτε θα καταλήγουν στα spam. Το όνομα αποστολέα και το email ρυθμίζονται στα πεδία `EMAIL_FROM_NAME` και `EMAIL_FROM_ADDRESS` (στο `.env`).
*   **Σύνδεση Βάσης Δεδομένων:** Το Connection String (`DB_CONNECTION_STRING`) για τον SQL Server όπου θα φιλοξενηθεί η βάση του KosmoReminder.

## 2. Βάση Δεδομένων & Συγχρονισμός (Slis DB)

Η βάση δεδομένων απαιτεί τα εξής βήματα (όπως περιγράφονται και στο README):
1.  **Εγκατάσταση Σχήματος:** Εκτέλεση των αρχείων `001`, `002` και `003` από τον φάκελο `sql/` στον SQL Server για τη δημιουργία της βάσης (`KosmoReminder`), του Linked Server, και των πινάκων.
2.  **Job Συγχρονισμού (Sync Job):** Έχει δημιουργηθεί ένα Stored Procedure (`sp_SyncAppointmentsFromSlis`) που αντλεί δεδομένα μέσω του Linked Server από τη βάση του Slis. **Απαιτείται** η ρύθμιση ενός **SQL Server Agent Job** που θα τρέχει αυτόματα αυτό το procedure κάθε 5 με 15 λεπτά. Έτσι θα ενημερώνονται συνεχώς τα νέα και αλλαγμένα ραντεβού στο KosmoReminder (ο κώδικας για το Job υπάρχει μέσα στο `sql/003_SyncStoredProcedure.sql`).

## 3. Στρατηγική Εγκατάστασης (Deployment Strategy)

Το σύστημα είναι σχεδιασμένο με αρχιτεκτονική **Κεντρικού Server (Client-Server)**. Αυτό σημαίνει ότι τα background services ΔΕΝ χρειάζεται (και δεν πρέπει) να τρέχουν σε κάθε υπολογιστή του διαγνωστικού κέντρου.

### Βήμα 3.1: Εγκατάσταση στον Κεντρικό Server
Στον κεντρικό υπολογιστή/διακομιστή (εκεί όπου ιδανικά τρέχει και ο SQL Server ή σε κάποιον application server στο ίδιο δίκτυο):
1.  Εγκαταστήστε την **Python 3.10+** και τις βιβλιοθήκες (`pip install -r src/requirements.txt`).
2.  Δημιουργήστε το αρχείο `src/.env` και συμπληρώστε τα κλειδιά που αναφέρθηκαν στο Βήμα 1.
3.  Τρέξτε το PowerShell script `install_services.ps1` (κάντε Δεξί Κλικ -> Run with PowerShell **ως Διαχειριστής / Administrator**).
    *Αυτό θα εγκαταστήσει τα 3 βασικά προγράμματα (Email Service, SMS/Viber Service, και το Web/Dashboard API) ως **Windows Services** (μέσω του εργαλείου NSSM) ώστε να τρέχουν πάντα αυτόματα στο παρασκήνιο.*

### Βήμα 3.2: Εγκατάσταση στους Υπολογιστές των Υπαλλήλων (Clients)
Για τους υπολογιστές των υπαλλήλων της Κοσμοϊατρικής (reception, κτλ) **δεν απαιτείται καμία εγκατάσταση της Python ή των Services**. 
Υπάρχουν δύο επιλογές για να έχουν πρόσβαση στο Dashboard:

**Επιλογή Α (Προτεινόμενη - Μέσω Web Browser):**
Δεδομένου ότι το UI είναι ένα Web Dashboard, οι χρήστες μπορούν απλά να ανοίξουν τον Google Chrome/Edge και να πληκτρολογήσουν την εσωτερική IP του κεντρικού Server (π.χ. `http://192.168.1.100:5000/`). Αυτό είναι το πιο εύκολο και δεν χρειάζεται κανένα update στους clients.

**Επιλογή Β (Μέσω του Desktop Client .exe):**
Αν θέλετε η εφαρμογή να εμφανίζεται σαν ένα ξεχωριστό desktop πρόγραμμα στο PC τους (και όχι σε tab του Chrome):
1. Στον Server ή στο περιβάλλον ανάπτυξης τρέξτε το `build.bat` για να δημιουργηθεί το εκτελέσιμο `KosmoReminder_Dashboard.exe`.
2. Αντιγράψτε **μόνο** αυτό το αρχείο `.exe` στους υπολογιστές των υπαλλήλων.
3. Επειδή το `.exe` εξ ορισμού προσπαθεί να συνδεθεί στο `localhost` (στον ίδιο υπολογιστή), θα πρέπει να φτιάξετε μια **Συντόμευση (Shortcut)** του `.exe` σε κάθε υπολογιστή και να προσθέσετε το URL του κεντρικού server στο πεδίο **Target (Προορισμός)**.
   *Παράδειγμα Target συντόμευσης:*
   `"C:\Διαδρομή\Προς\KosmoReminder_Dashboard.exe" http://192.168.1.100:5000/`

Με αυτόν τον τρόπο, όλοι οι υπάλληλοι θα βλέπουν, θα διαχειρίζονται, και θα κάνουν "Retries" στα **ίδια** ραντεβού σε πραγματικό χρόνο, συνδεδεμένοι κεντρικά στον Server της κλινικής.
